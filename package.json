{
  "name": "stormbrowser-v3",
  "version": "1.2.2",
  "description": "StormBrowser - A modern browser by StormGamesStudios","workspaces": [
    "browser",
    "setup"
  ],
  "private": true,
  "scripts": {
    "start": "npm run start --workspace=browser",
    "clean": "npm run clean --workspace=browser && npm run clean --workspace=setup",
    "build:win": "npm run build:win --workspace=browser && npm run build --workspace=setup",
    "build:linux": "npm run build:linux --workspace=browser",
    "build:all": "npm run build:all --workspace=browser && npm run build --workspace=setup",
    "build:setup": "npm run build --workspace=setup"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/acierto-incomodo/StormBrowser-V3.git"
  },
  "author": "StormGamesStudios <meliodas.aitor@gmail.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/acierto-incomodo/StormBrowser-V3/issues"
  },
  "homepage": "https://github.com/acierto-incomodo/StormBrowser-V3#readme"
}
